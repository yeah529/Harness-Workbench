# Optional Codex route. It is passed only when the launcher has an explicit
# token source; it never changes DSH's deepseek-official default model.
- id: llm-pi-ai
  name: '@deepseek-ai/dsh-llm-pi-ai'
  config:
    providers:
      openai-codex:
        apiKeyEnv: OPENAI_CODEX_ACCESS_TOKEN
